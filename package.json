{
  "name": "marbles",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "lzutf8": "^0.4.7",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.0"
  },
  "homepage": "https://hanhee-song.github.io/marble_machine",
  "scripts": {
    "sass": "node-sass scss -o public --watch",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
